# Per-OS caller → its own status badge (label = "mac"). Real logic lives in ci-core.yml.
name: mac

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: ./.github/workflows/ci-core.yml
    with:
      os: macos-latest
