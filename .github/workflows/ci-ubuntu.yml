# Per-OS caller → its own status badge (label = "ubuntu"). Real logic lives in ci-core.yml.
name: ubuntu

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: ./.github/workflows/ci-core.yml
    with:
      os: ubuntu-latest
